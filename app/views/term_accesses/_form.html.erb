<%= form_with(model: [@school, @student, @term, @term_access]) do |form| %>
  <% if term_access.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(term_access.errors.count, "error") %> prohibited this term_access from being saved:</h2>

      <ul>
        <% term_access.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>

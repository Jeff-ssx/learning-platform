<p style="color: green"><%= notice %></p>

<h1>Schools</h1>

<div id="schools">
  <% @schools.each do |school| %>
    <%= school.name %>
    <p>
      <%= link_to "Login with this school", school_students_login_path(school) %>
    </p>
  <% end %>
</div>
